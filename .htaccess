<IfModule mod_rewrite.c>
    RewriteEngine On
    # Solo si Document Root = raíz Laravel (no .../public). Sin Options (Namecheap 500).
    RewriteRule ^$ public/ [L]
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ public/$1 [L]
</IfModule>
